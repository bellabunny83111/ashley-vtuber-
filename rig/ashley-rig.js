(()=>{const C=(n,a,b)=>Math.max(a,Math.min(b,n)),r=document.querySelector('[data-ashley-rig]');if(!r)return;const b={};r.querySelectorAll('[data-bone]').forEach(e=>b[e.dataset.bone]=e);let s={x:0,y:0,mouth:0},t={x:0,y:0};const bone=(n,x=0,y=0,z=0)=>{if(b[n])b[n].style.transform=`translate(${x}px,${y}px) rotate(${z}deg)`};function draw(){s.x+=(t.x-s.x)*.12;s.y+=(t.y-s.y)*.12;bone('root',s.x*10,s.y*6,s.x*1.2);bone('torso',s.x*2,Math.sin(performance.now()/850)*1.8,s.x*.8);bone('head',s.x*7,s.y*4,s.x*6);bone('hairL',0,0,-s.x*3+Math.sin(performance.now()/600)*1.4);bone('hairR',0,0,-s.x*3-Math.sin(performance.now()/620)*1.4);bone('earL',0,0,-s.x*4+Math.sin(performance.now()/500)*2);bone('earR',0,0,-s.x*4-Math.sin(performance.now()/520)*2);r.style.setProperty('--mouth-open',s.mouth);requestAnimationFrame(draw)}draw();window.AshleyRig={look(x,y){t.x=C(x,-1,1);t.y=C(y,-1,1)},mouth(v){s.mouth=C(v,0,1)},pose(n){r.dataset.pose=n;clearTimeout(this.p);this.p=setTimeout(()=>r.dataset.pose='idle',1400)},reset(){t={x:0,y:0};s.mouth=0;r.dataset.pose='idle'}};})();