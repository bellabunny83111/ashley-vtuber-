(()=>{let face,last=-1,running=false,raf=0,busy=false,failures=0,lastSeen=0,activeVideo=null,activeStatus=()=>{},suspended=false;async function init(){if(face)return face;const v=await import('https://cdn.jsdelivr.net/npm/@mediapipe/tasks-vision@0.10.22/+esm');const fs=await v.FilesetResolver.forVisionTasks('https://cdn.jsdelivr.net/npm/@mediapipe/tasks-vision@0.10.22/wasm');face=await v.FaceLandmarker.createFromOptions(fs,{baseOptions:{modelAssetPath:'https://storage.googleapis.com/mediapipe-models/face_landmarker/face_landmarker/float16/1/face_landmarker.task'},runningMode:'VIDEO',numFaces:1,minFaceDetectionConfidence:.5,minFacePresenceConfidence:.5,minTrackingConfidence:.5,outputFaceBlendshapes:true,outputFacialTransformationMatrixes:true});return face}function schedule(){if(!running||suspended||raf)return;raf=requestAnimationFrame(loop)}function loop(){raf=0;if(!running||suspended)return;const video=activeVideo,onStatus=activeStatus;schedule();if(!video||busy||document.hidden||video.readyState<2||video.currentTime===last)return;busy=true;try{last=video.currentTime;const r=face.detectForVideo(video,performance.now()),lm=r.faceLandmarks?.[0];if(lm){lastSeen=performance.now();failures=0;const n=lm[1],l=lm[33],rr=lm[263],cx=(l.x+rr.x)/2,cy=(l.y+rr.y)/2;let p={x:(n.x-cx)*8,y:(n.y-cy-.10)*6};p=window.BellabunnyCalibration?.map(p.x,p.y)||p;BellabunnyRig.batch({headX:p.x,headY:p.y,...BellabunnyRig.fromBlendshapes(r.faceBlendshapes?.[0]?.categories||[])});onStatus(true,r)}else onStatus(false,r)}catch(e){failures++;if(failures===1||failures%30===0)console.warn('Bellabunny tracking frame failed',e);onStatus(false,{error:e})}finally{busy=false}}async function start(video,onStatus=()=>{}){await init();activeVideo=video;activeStatus=onStatus;if(running){resume();return}running=true;failures=0;last=-1;suspended=document.hidden;schedule()}function suspend(){if(!running)return;suspended=true;if(raf)cancelAnimationFrame(raf);raf=0;busy=false;last=-1}function resume(){if(!running||document.hidden)return;suspended=false;busy=false;last=-1;const video=activeVideo;if(video?.paused&&video.srcObject)video.play().catch(()=>{});schedule()}function stop(){running=false;suspended=false;if(raf)cancelAnimationFrame(raf);raf=0;busy=false;last=-1;activeVideo=null;activeStatus=()=>{}}function destroy(){stop();face?.close?.();face=null}document.addEventListener('visibilitychange',()=>document.hidden?suspend():resume());window.addEventListener('pagehide',suspend);window.addEventListener('pageshow',resume);window.BellabunnyFaceTracker={init,start,stop,destroy,suspend,resume,get running(){return running},get lastSeen(){return lastSeen}}})();