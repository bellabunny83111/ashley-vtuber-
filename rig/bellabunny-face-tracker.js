(()=>{let face,last=-1,running=false;async function init(){if(face)return face;const v=await import('https://cdn.jsdelivr.net/npm/@mediapipe/tasks-vision@0.10.22/+esm');const fs=await v.FilesetResolver.forVisionTasks('https://cdn.jsdelivr.net/npm/@mediapipe/tasks-vision@0.10.22/wasm');face=await v.FaceLandmarker.createFromOptions(fs,{baseOptions:{modelAssetPath:'https://storage.googleapis.com/mediapipe-models/face_landmarker/face_landmarker/float16/1/face_landmarker.task'},runningMode:'VIDEO',numFaces:1,minFaceDetectionConfidence:.5,minFacePresenceConfidence:.5,minTrackingConfidence:.5,outputFaceBlendshapes:true,outputFacialTransformationMatrixes:true});return face}async function start(video,onStatus=()=>{}){await init();running=true;async function loop(){if(!running)return;if(video.readyState>=2&&video.currentTime!==last){last=video.currentTime;const r=face.detectForVideo(video,performance.now()),lm=r.faceLandmarks?.[0];if(lm){const n=lm[1],l=lm[33],rr=lm[263],cx=(l.x+rr.x)/2,cy=(l.y+rr.y)/2;BellabunnyRig.batch({headX:Math.max(-1,Math.min(1,(n.x-cx)*8)),headY:Math.max(-1,Math.min(1,(n.y-cy-.10)*6)),...BellabunnyRig.fromBlendshapes(r.faceBlendshapes?.[0]?.categories||[])});onStatus(true)}else onStatus(false)}requestAnimationFrame(loop)}loop()}window.BellabunnyFaceTracker={init,start,stop(){running=false}}})();