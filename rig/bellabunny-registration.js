(()=>{const KEY='bellabunny.registration.v2',CANONICAL_URL='../assets/ashley/layers/registration.json';let data={},canonical={},canonicalReady=false;try{data=JSON.parse(localStorage.getItem(KEY)||'{}')}catch{}const norm=v=>({x:Number.isFinite(+v?.x)?+v.x:0,y:Number.isFinite(+v?.y)?+v.y:0,scale:Number.isFinite(+v?.scale)&&+v.scale>0?+v.scale:1,rotate:Number.isFinite(+v?.rotate)?+v.rotate:0});function get(name){return norm(has(name)?data[name]:canonical[name])}function has(name){return Object.prototype.hasOwnProperty.call(data,name)}function hasCanonical(name){return Object.prototype.hasOwnProperty.call(canonical,name)}function isRegistered(name){return has(name)||hasCanonical(name)}function set(name,v){data[name]=norm(v);localStorage.setItem(KEY,JSON.stringify(data));apply(name);dispatchEvent(new CustomEvent('bellabunny:registration',{detail:{name,value:data[name],source:'local'}}));return data[name]}function apply(name){const el=document.querySelector('[data-layer="'+name+'"]');if(!el)return;const v=get(name);el.style.setProperty('--reg-x',v.x+'px');el.style.setProperty('--reg-y',v.y+'px');el.style.setProperty('--reg-scale',v.scale);el.style.setProperty('--reg-rotate',v.rotate+'deg');el.dataset.registered=isRegistered(name)?'true':'false';el.dataset.registrationSource=has(name)?'local':hasCanonical(name)?'canonical':'default'}function importData(raw){let next=raw;try{if(typeof raw==='string')next=JSON.parse(raw)}catch{return false}if(!next||typeof next!=='object'||Array.isArray(next))return false;const clean={};for(const [k,v] of Object.entries(next))clean[k]=norm(v);data=clean;localStorage.setItem(KEY,JSON.stringify(data));applyAll();dispatchEvent(new CustomEvent('bellabunny:registration',{detail:{imported:Object.keys(clean).length}}));return true}function applyAll(){document.querySelectorAll('[data-layer]').forEach(el=>apply(el.dataset.layer))}function reset(name){if(name)delete data[name];else data={};localStorage.setItem(KEY,JSON.stringify(data));applyAll();dispatchEvent(new CustomEvent('bellabunny:registration',{detail:{reset:name||'all'}}))}function seed(names=[]){for(const n of names)if(!has(n))data[n]=norm({});localStorage.setItem(KEY,JSON.stringify(data));applyAll();dispatchEvent(new CustomEvent('bellabunny:registration',{detail:{seeded:names}}))}async function loadCanonical(){try{const r=await fetch(CANONICAL_URL,{cache:'no-cache'});if(!r.ok)throw new Error('HTTP '+r.status);const raw=await r.json(),layers=raw?.layers;if(!layers||typeof layers!=='object'||Array.isArray(layers))throw new Error('Invalid canonical registration');const clean={};for(const [k,v] of Object.entries(layers))clean[k]=norm(v);canonical=clean;canonicalReady=true;applyAll();dispatchEvent(new CustomEvent('bellabunny:registration',{detail:{canonical:Object.keys(clean).length,ready:true}}));return true}catch(error){canonical={};canonicalReady=false;dispatchEvent(new CustomEvent('bellabunny:registration',{detail:{canonical:0,ready:false,error:String(error?.message||error)}}));return false}}addEventListener('bellabunny:layers',applyAll);window.BellabunnyRegistration={get,has,hasCanonical,isRegistered,set,seed,apply,applyAll,reset,import:importData,export:()=>JSON.stringify(data,null,2),loadCanonical,status:()=>({canonicalReady,canonicalCount:Object.keys(canonical).length,localCount:Object.keys(data).length})};loadCanonical()})();